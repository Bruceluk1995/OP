interface:
  display_name: "JP Isekai Write"
  short_description: "Draft push-ready Japanese male isekai episodes"
  default_prompt: "Use $jp-isekai-write to draft a first-person Japanese isekai episode from a full episode blueprint: plot-bomb opening, previous-episode bridge, dense/sparse payoff beats, cost/reward hook, JP anti-AI gates, local script checks for saved files, and package files when needed."
