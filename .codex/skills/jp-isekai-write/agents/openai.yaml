interface:
  display_name: "JP Isekai Write"
  short_description: "Draft novel or push-style Japanese male isekai"
  default_prompt: "Use $jp-isekai-write to confirm format, randomly draw and fill one screenshot-derived push-opening card, then write the Japanese male-isekai version with quality gates."
